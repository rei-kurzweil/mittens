[1mdiff --git a/src/engine/ecs/component/mod.rs b/src/engine/ecs/component/mod.rs[m
[1mindex 63e1cd9..3157a39 100644[m
[1m--- a/src/engine/ecs/component/mod.rs[m
[1m+++ b/src/engine/ecs/component/mod.rs[m
[36m@@ -46,6 +46,7 @@[m [mpub mod keyframe;[m
 pub mod kinetic_response;[m
 pub mod light_quantization;[m
 pub mod mesh;[m
[32m+[m[32mpub mod music_context;[m
 pub mod music_note;[m
 pub mod opacity;[m
 pub mod overlay;[m
[36m@@ -137,6 +138,7 @@[m [mpub use keyframe::KeyframeComponent;[m
 pub use kinetic_response::{KineticResponseComponent, KineticResponseMode};[m
 pub use light_quantization::LightQuantizationComponent;[m
 pub(crate) use music_note::NotePitch;[m
[32m+[m[32mpub use music_context::MusicContextComponent;[m
 pub use music_note::{MusicNote, MusicNoteComponent};[m
 pub use opacity::OpacityComponent;[m
 pub use openxr::OpenXRComponent;[m
[1mdiff --git a/src/engine/ecs/component/music_note.rs b/src/engine/ecs/component/music_note.rs[m
[1mindex c0d40ca..c47f828 100644[m
[1m--- a/src/engine/ecs/component/music_note.rs[m
[1m+++ b/src/engine/ecs/component/music_note.rs[m
[36m@@ -186,6 +186,13 @@[m [mpub struct MusicNoteComponent {[m
     #[serde(default)][m
     pub play_on_attach: bool,[m
 [m
[32m+[m[32m    /// Voice name for `MusicContext` lookup (rank 3 / rank 4 in the[m
[32m+[m[32m    /// resolution precedence). Used only when `target_source` is None;[m
[32m+[m[32m    /// matches against the nearest ancestor `MusicContextComponent`'s[m
[32m+[m[32m    /// `voices` map. `None` means "use the context's first voice".[m
[32m+[m[32m    #[serde(default)][m
[32m+[m[32m    pub voice_name: Option<String>,[m
[32m+[m
     #[serde(skip)][m
     component: Option<ComponentId>,[m
 }[m
[36m@@ -198,10 +205,16 @@[m [mimpl MusicNoteComponent {[m
             target_resolved: None,[m
             scheduled_beat: None,[m
             play_on_attach: false,[m
[32m+[m[32m            voice_name: None,[m
             component: None,[m
         }[m
     }[m
 [m
[32m+[m[32m    pub fn with_voice_name(mut self, name: impl Into<String>) -> Self {[m
[32m+[m[32m        self.voice_name = Some(name.into());[m
[32m+[m[32m        self[m
[32m+[m[32m    }[m
[32m+[m
     pub fn from_note(note: MusicNote) -> Self {[m
         Self::new(note)[m
     }[m
[36m@@ -225,18 +238,81 @@[m [mimpl MusicNoteComponent {[m
         self.component[m
     }[m
 [m
[31m-    /// Resolve `target_source` into `target_resolved` and return it.[m
[31m-    /// Returns the cached id immediately if already resolved.[m
[31m-    /// Returns `None` when no `target_source` is set — the executor's[m
[31m-    /// ancestor-walk fallback handles that case and writes back the[m
[31m-    /// resolved id to `target_resolved` afterward.[m
[31m-    pub fn resolve_target(&mut self, world: &World) -> Option<ComponentId> {[m
[32m+[m[32m    /// Resolve a target for this note following docs/spec/audio-sources.md[m
[32m+[m[32m    /// §6.6 precedence: cached → `target_source` (Guid/Query) → nearest[m
[32m+[m[32m    /// ancestor `MusicContextComponent` (named voice if `voice_name` set,[m
[32m+[m[32m    /// else voice 0). Returns `None` when nothing resolves — the[m
[32m+[m[32m    /// executor's ancestor-walk fallback (rank 5) handles that case and[m
[32m+[m[32m    /// writes the result back to `target_resolved` afterward.[m
[32m+[m[32m    pub fn resolve_target(&mut self, world: &mut World) -> Option<ComponentId> {[m
         if self.target_resolved.is_some() {[m
             return self.target_resolved;[m
         }[m
[31m-        let src = self.target_source.as_ref()?;[m
[31m-        let resolved = match src {[m
[31m-            ComponentRef::Guid(uuid) => world.component_id_by_guid(*uuid),[m
[32m+[m[32m        if let Some(src) = self.target_source.as_ref() {[m
[32m+[m[32m            let resolved = match src {[m
[32m+[m[32m                ComponentRef::Guid(uuid) => world.component_id_by_guid(*uuid),[m
[32m+[m[32m                ComponentRef::Query(selector) => {[m
[32m+[m[32m                    let roots: Vec<ComponentId> = world[m
[32m+[m[32m                        .all_components()[m
[32m+[m[32m                        .filter(|&cid| world.parent_of(cid).is_none())[m
[32m+[m[32m                        .collect();[m
[32m+[m[32m                    roots[m
[32m+[m[32m                        .into_iter()[m
[32m+[m[32m                        .find_map(|root| world.find_component(root, selector))[m
[32m+[m[32m                }[m
[32m+[m[32m            };[m
[32m+[m[32m            self.target_resolved = resolved;[m
[32m+[m[32m            if resolved.is_some() {[m
[32m+[m[32m                return resolved;[m
[32m+[m[32m            }[m
[32m+[m[32m        }[m
[32m+[m[32m        // Rank 4: walk ancestors for the nearest MusicContextComponent and[m
[32m+[m[32m        // look up our voice (named, or position 0 when unnamed).[m
[32m+[m[32m        let self_id = self.component?;[m
[32m+[m[32m        let ctx_id = find_music_context_ancestor(world, self_id)?;[m
[32m+[m[32m        let voice = self.voice_name.clone();[m
[32m+[m[32m        let resolved = {[m
[32m+[m[32m            let ctx = world[m
[32m+[m[32m                .get_component_by_id_as_mut::<super::MusicContextComponent>(ctx_id)?;[m
[32m+[m[32m            // Defer the lookup: cache check is cheap, but ref resolution[m
[32m+[m[32m            // needs `&World`, which we already borrowed mutably above.[m
[32m+[m[32m            let cached = match &voice {[m
[32m+[m[32m                None => ctx.voices_resolved.first().copied().flatten(),[m
[32m+[m[32m                Some(n) => ctx[m
[32m+[m[32m                    .voices[m
[32m+[m[32m                    .iter()[m
[32m+[m[32m                    .position(|(vn, _)| vn == n)[m
[32m+[m[32m                    .and_then(|i| ctx.voices_resolved.get(i).copied().flatten()),[m
[32m+[m[32m            };[m
[32m+[m[32m            if cached.is_some() {[m
[32m+[m[32m                cached[m
[32m+[m[32m            } else {[m
[32m+[m[32m                None[m
[32m+[m[32m            }[m
[32m+[m[32m        };[m
[32m+[m[32m        if let Some(r) = resolved {[m
[32m+[m[32m            self.target_resolved = Some(r);[m
[32m+[m[32m            return Some(r);[m
[32m+[m[32m        }[m
[32m+[m[32m        // Cold path: ask the context to resolve + cache. Re-borrow.[m
[32m+[m[32m        let resolved = {[m
[32m+[m[32m            let ctx = world[m
[32m+[m[32m                .get_component_by_id_as_mut::<super::MusicContextComponent>(ctx_id)?;[m
[32m+[m[32m            // SAFETY of borrow: lookup_voice needs &World too. We grab voice[m
[32m+[m[32m            // entries by clone, drop the mut borrow, then read-only world.[m
[32m+[m[32m            let entry = match &voice {[m
[32m+[m[32m                None => ctx.voices.first().cloned(),[m
[32m+[m[32m                Some(n) => ctx[m
[32m+[m[32m                    .voices[m
[32m+[m[32m                    .iter()[m
[32m+[m[32m                    .find(|(vn, _)| vn == n)[m
[32m+[m[32m                    .cloned(),[m
[32m+[m[32m            };[m
[32m+[m[32m            entry[m
[32m+[m[32m        };[m
[32m+[m[32m        let (_, src) = resolved?;[m
[32m+[m[32m        let id = match src {[m
[32m+[m[32m            ComponentRef::Guid(uuid) => world.component_id_by_guid(uuid),[m
             ComponentRef::Query(selector) => {[m
                 let roots: Vec<ComponentId> = world[m
                     .all_components()[m
[36m@@ -244,12 +320,41 @@[m [mimpl MusicNoteComponent {[m
                     .collect();[m
                 roots[m
                     .into_iter()[m
[31m-                    .find_map(|root| world.find_component(root, selector))[m
[32m+[m[32m                    .find_map(|root| world.find_component(root, &selector))[m
             }[m
         };[m
[31m-        self.target_resolved = resolved;[m
[31m-        resolved[m
[32m+[m[32m        // Cache into the context too, so other notes benefit.[m
[32m+[m[32m        if let (Some(id), Some(ctx)) = ([m
[32m+[m[32m            id,[m
[32m+[m[32m            world.get_component_by_id_as_mut::<super::MusicContextComponent>(ctx_id),[m
[32m+[m[32m        ) {[m
[32m+[m[32m            let idx = match &voice {[m
[32m+[m[32m                None => Some(0),[m
[32m+[m[32m                Some(n) => ctx.voices.iter().position(|(vn, _)| vn == n),[m
[32m+[m[32m            };[m
[32m+[m[32m            if let Some(idx) = idx {[m
[32m+[m[32m                if let Some(slot) = ctx.voices_resolved.get_mut(idx) {[m
[32m+[m[32m                    *slot = Some(id);[m
[32m+[m[32m                }[m
[32m+[m[32m            }[m
[32m+[m[32m        }[m
[32m+[m[32m        self.target_resolved = id;[m
[32m+[m[32m        id[m
[32m+[m[32m    }[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfn find_music_context_ancestor(world: &World, id: ComponentId) -> Option<ComponentId> {[m
[32m+[m[32m    let mut cur = id;[m
[32m+[m[32m    while let Some(p) = world.parent_of(cur) {[m
[32m+[m[32m        if world[m
[32m+[m[32m            .get_component_by_id_as::<super::MusicContextComponent>(p)[m
[32m+[m[32m            .is_some()[m
[32m+[m[32m        {[m
[32m+[m[32m            return Some(p);[m
[32m+[m[32m        }[m
[32m+[m[32m        cur = p;[m
     }[m
[32m+[m[32m    None[m
 }[m
 [m
 impl Default for MusicNoteComponent {[m
[1mdiff --git a/src/engine/ecs/rx/intent_executor.rs b/src/engine/ecs/rx/intent_executor.rs[m
[1mindex 094ed35..c92e3b6 100644[m
[1m--- a/src/engine/ecs/rx/intent_executor.rs[m
[1m+++ b/src/engine/ecs/rx/intent_executor.rs[m
[36m@@ -555,11 +555,33 @@[m [mfn handle_intent_signal(world: &mut World, emit: &mut dyn SignalEmitter, env: &S[m
 [m
             let mut osc_cids = Vec::new();[m
             for &t in component_ids.iter() {[m
[32m+[m[32m                // When the target is a MusicNoteComponent, run its full[m
[32m+[m[32m                // resolution chain (cache → target_source → context voice)[m
[32m+[m[32m                // before the ancestor-walk fallback. See[m
[32m+[m[32m                // docs/spec/audio-sources.md §6.6.[m
[32m+[m[32m                let mut resolved_via_note: Option<ComponentId> = None;[m
[32m+[m[32m                if world.get_component_by_id_as::<MusicNoteComponent>(t).is_some() {[m
[32m+[m[32m                    // Take the component out, resolve (needs &mut World), put back.[m
[32m+[m[32m                    let mut taken: MusicNoteComponent = world[m
[32m+[m[32m                        .get_component_by_id_as::<MusicNoteComponent>(t)[m
[32m+[m[32m                        .cloned()[m
[32m+[m[32m                        .unwrap();[m
[32m+[m[32m                    resolved_via_note = taken.resolve_target(world);[m
[32m+[m[32m                    if let Some(slot) =[m
[32m+[m[32m                        world.get_component_by_id_as_mut::<MusicNoteComponent>(t)[m
[32m+[m[32m                    {[m
[32m+[m[32m                        slot.target_resolved = taken.target_resolved;[m
[32m+[m[32m                    }[m
[32m+[m[32m                }[m
[32m+[m[32m                if let Some(via_note) = resolved_via_note {[m
[32m+[m[32m                    osc_cids.push(via_note);[m
[32m+[m[32m                    continue;[m
[32m+[m[32m                }[m
[32m+[m
                 let before = osc_cids.len();[m
                 collect_oscillator_targets(world, t, &mut osc_cids);[m
[31m-                // Cache the resolved oscillator back into the MusicNoteComponent[m
[31m-                // when this dispatch originated from one with no pre-resolved[m
[31m-                // target. Subsequent fires skip the ancestor walk.[m
[32m+[m[32m                // Cache the ancestor-walk result back into the[m
[32m+[m[32m                // MusicNoteComponent so subsequent fires skip the walk.[m
                 if let Some(&found) = osc_cids[before..].first() {[m
                     if let Some(mn) =[m
                         world.get_component_by_id_as_mut::<MusicNoteComponent>(t)[m
[1mdiff --git a/src/meow_meow/component_registry.rs b/src/meow_meow/component_registry.rs[m
[1mindex edaf618..7575a19 100644[m
[1m--- a/src/meow_meow/component_registry.rs[m
[1m+++ b/src/meow_meow/component_registry.rs[m
[36m@@ -33,7 +33,7 @@[m [muse crate::engine::ecs::component::{[m
     TransformSampleAncestorComponent,[m
     BoundsComponent, MeshComponent, GestureCoordTypeComponent, GestureCoordType,[m
     CollisionShapeComponent, CollisionShape, CollisionComponent, CollisionMode,[m
[31m-    GravityComponent, MusicNote, MusicNoteComponent,[m
[32m+[m[32m    GravityComponent, MusicNote, MusicNoteComponent, MusicContextComponent,[m
     Vector3TemporalFilterComponent, QuatYawFollowComponent,[m
     SignalRouteUpwardComponent, SkinnedMeshComponent, RayCastComponent, RayCastMode,[m
     RaycastableShapeComponent, RaycastableShapeType, IKChainComponent, IKSolver,[m
[36m@@ -1224,7 +1224,23 @@[m [mfn create_component([m
                 "g" => MusicNote::g(octave, duration),[m
                 _ => MusicNote::default(),[m
             };[m
[31m-            add!(MusicNoteComponent::new(note))[m
[32m+[m[32m            let mut mn = MusicNoteComponent::new(note);[m
[32m+[m[32m            // Optional 3rd positional arg: either a voice name ("bass") or[m
[32m+[m[32m            // a component ref. Per docs/spec/audio-sources.md §6.6 rank 3.[m
[32m+[m[32m            if let Ok(v) = arg(args, 2) {[m
[32m+[m[32m                match v {[m
[32m+[m[32m                    Value::String(s) => mn.voice_name = Some(s.clone()),[m
[32m+[m[32m                    _ => {[m
[32m+[m[32m                        if let Ok(src) = value_to_component_ref(world, v) {[m
[32m+[m[32m                            mn.target_source = Some(src);[m
[32m+[m[32m                        }[m
[32m+[m[32m                    }[m
[32m+[m[32m                }[m
[32m+[m[32m            }[m
[32m+[m[32m            add!(mn)[m
[32m+[m[32m        }[m
[32m+[m[32m        "MusicContext" => {[m
[32m+[m[32m            add!(MusicContextComponent::new())[m
         }[m
         "IKChain" => {[m
             let solver = match ctor {[m
[36m@@ -1490,21 +1506,52 @@[m [mfn apply_call([m
         }[m
         return Ok(());[m
     }[m
[31m-    if let Some(mn) = world.get_component_by_id_as_mut::<MusicNoteComponent>(id) {[m
[32m+[m[32m    if world.get_component_by_id_as::<MusicNoteComponent>(id).is_some() {[m
         match method {[m
             "velocity" => {[m
[31m-                mn.note = mn.note.with_velocity(arg_f32(args, 0)?);[m
[32m+[m[32m                if let Some(mn) = world.get_component_by_id_as_mut::<MusicNoteComponent>(id) {[m
[32m+[m[32m                    mn.note = mn.note.with_velocity(arg_f32(args, 0)?);[m
[32m+[m[32m                }[m
             }[m
             "play_on_attach" => {[m
[31m-                mn.play_on_attach = true;[m
[32m+[m[32m                if let Some(mn) = world.get_component_by_id_as_mut::<MusicNoteComponent>(id) {[m
[32m+[m[32m                    mn.play_on_attach = true;[m
[32m+[m[32m                }[m
             }[m
             "at_beat" => {[m
[31m-                mn.scheduled_beat = Some(arg_f32(args, 0)? as f64);[m
[32m+[m[32m                let b = arg_f32(args, 0)? as f64;[m
[32m+[m[32m                if let Some(mn) = world.get_component_by_id_as_mut::<MusicNoteComponent>(id) {[m
[32m+[m[32m                    mn.scheduled_beat = Some(b);[m
[32m+[m[32m                }[m
[32m+[m[32m            }[m
[32m+[m[32m            "voice" => {[m
[32m+[m[32m                // voice("bass") — name lookup against MusicContext ancestor.[m
[32m+[m[32m                let name = val_as_str(arg(args, 0)?)?.to_string();[m
[32m+[m[32m                if let Some(mn) = world.get_component_by_id_as_mut::<MusicNoteComponent>(id) {[m
[32m+[m[32m                    mn.voice_name = Some(name);[m
[32m+[m[32m                }[m
[32m+[m[32m            }[m
[32m+[m[32m            "target" => {[m
[32m+[m[32m                // target(ref) — explicit ComponentRef override.[m
[32m+[m[32m                let src = arg_component_ref(world, args, 0)?;[m
[32m+[m[32m                if let Some(mn) = world.get_component_by_id_as_mut::<MusicNoteComponent>(id) {[m
[32m+[m[32m                    mn.target_source = Some(src);[m
[32m+[m[32m                }[m
             }[m
             _ => {}[m
         }[m
         return Ok(());[m
     }[m
[32m+[m[32m    if world.get_component_by_id_as::<MusicContextComponent>(id).is_some() {[m
[32m+[m[32m        if method == "voice" {[m
[32m+[m[32m            let name = val_as_str(arg(args, 0)?)?.to_string();[m
[32m+[m[32m            let src = arg_component_ref(world, args, 1)?;[m
[32m+[m[32m            if let Some(mc) = world.get_component_by_id_as_mut::<MusicContextComponent>(id) {[m
[32m+[m[32m                mc.add_voice(name, src);[m
[32m+[m[32m            }[m
[32m+[m[32m        }[m
[32m+[m[32m        return Ok(());[m
[32m+[m[32m    }[m
     if world.get_component_by_id_as::<IKChainComponent>(id).is_some() {[m
         match method {[m
             "weight" => {[m
