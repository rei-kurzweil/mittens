# Moved

The canonical spec for the implemented runtime-texture bridge now lives in [docs/spec/render-to-texture.md](docs/spec/render-to-texture.md).

This file remains only as a compatibility redirect for older links.