# Skinned mesh system (moved)

This doc has been superseded by:

- `docs/spec/skinned-mesh-system.md`

The new doc covers the full runtime pipeline (systems + signals), and also documents how skinning relates to transforms, gizmos, and intent routing.
